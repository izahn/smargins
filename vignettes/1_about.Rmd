---
title: "About smargins"
date: "`r Sys.Date()`"
output: rmarkdown::html_vignette
vignette: >
  %\VignetteIndexEntry{About}
  %\VignetteEngine{knitr::rmarkdown}
  %\VignetteEncoding{UTF-8}
---
  
  ```{r setup, include=FALSE}
knitr::opts_knit$set(
  stop_on_error = 2L
)
```

## Simulation Based Average Marginal Effects

The `smargins` package calculates AMEs, similar to Stata's `margins` command and the `effects` and `margins` packages in R. The focus is on using simulations to quantify uncertainty, in the style of `Zelig`.
